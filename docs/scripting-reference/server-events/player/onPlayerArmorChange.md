---
title: 'onPlayerArmorChange'
---
# `event` onPlayerArmorChange <font size="4">(server-side)</font>
!!! info "Available since version: 0.3.0"

Triggered when a player's armor changes.

## Parameters
```c++
void onPlayerArmorChange(number player_id, number|nil instance)
```

* `number` **player_id**: Player id.
* `number|nil` **instance**: New armor instance id (nil if none).
