---
title: 'isHudEnabled'
---
# `function` isHudEnabled <font size="4">(client-side)</font>
!!! info "Available since version: 0.3.0"

This function will return whether the Gothic HUD is enabled.

## Declaration
```cpp
boolean|nil isHudEnabled(number hud_type)
```

## Parameters
* `number` **hud_type**: HUD type constant.
  
## Returns `boolean|nil`
HUD state, or nil for unsupported HUD types.
